version: "3.3"

services:
  print:
    image: docker.pkg.github.com/khonkaen-hospital/print-label/print-label:latest
    ports:
      - 7007:3000
    volumes:
      - ./.env:/app/.env
    networks:
      - brokernet
networks:
  brokernet:
    driver: bridge
