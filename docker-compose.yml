version: "3.3"

services:
  print:
    image: dixonsatit/print-label:1.0.2
    ports:
      - 7007:3000
    volumes:
      - ./.env:/app/.env
    networks:
      - brokernet
networks:
  brokernet:
    driver: bridge
